Read the repository's contributor instructions before planning. Inspect the files that implement the current behavior and the tests that define its public contract.

Return a Markdown plan with these sections:

1. `Goal` - Restate the intended behavior and constraints.
2. `Current design` - Summarize the relevant architecture with exact file paths and symbols.
3. `Implementation steps` - Provide an ordered list of concrete changes. Include the location, behavior, rationale, and dependencies of each step.
4. `Validation` - List the public behaviors, failure cases, and commands that must verify the implementation.
5. `Risks and open questions` - Identify compatibility, migration, security, performance, or product decisions that remain unresolved.

Do not pad the plan with generic tasks. Do not claim to have inspected a file that you did not read. Do not modify the workspace.
